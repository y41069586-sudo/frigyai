workflows:
  android-build:
    name: Android Build

    environment:
      node: latest

    scripts:
      - npm install
      - npm install -g eas-cli
      - eas build --platform android --non-interactive

    artifacts:
      - build/**/*.apk
      - build/**/*.aab
      environment_variables:
  EXPO_TOKEN: b2TWZmLz0pJk1MgXxPR939cl3SLF8ApTxqqWoXTt
