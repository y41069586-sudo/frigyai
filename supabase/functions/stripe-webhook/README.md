# stripe-webhook

Affiliate helpers are **inlined in `index.ts`** so Supabase Dashboard deploy (single file) works.

Edit affiliate logic in `index.ts` (search for `Affiliate helpers`).

Other functions still use `./affiliate.ts` in their own folders.
