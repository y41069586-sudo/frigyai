/** Trial length configured in App Store Connect / Play Console (must match store products). */
export const SUBSCRIPTION_TRIAL_DAYS = 3;

export const SUBSCRIPTION_PRICING = {
  trialDays: SUBSCRIPTION_TRIAL_DAYS,
} as const;
